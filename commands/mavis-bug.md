---
description: "Bug mode — scientific debugging protocol"
argument-hint: "[bug description]"
---

Invoke the `mavis:bug` skill directly with: "$ARGUMENTS"

Bypasses the smart router — goes straight to Bug Mode (reproduce → hypothesize → isolate → fix → verify).
