---
description: "Quick mode — fast single-agent execution for simple changes"
argument-hint: "[task]"
---

Invoke the `mavis:quick` skill directly with: "$ARGUMENTS"

Bypasses the smart router — goes straight to Quick Mode (0-3 questions, single agent, lint/test verify).
