---
description: "Start MAVIS — intelligent development harness with auto-mode detection"
argument-hint: "[requirement]"
---

Invoke the `mavis:start` skill with the user's input: "$ARGUMENTS"

The skill will analyze the input, detect the appropriate execution mode (Full/Quick/Bug), and orchestrate the workflow end-to-end.
